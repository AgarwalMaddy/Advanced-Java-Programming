package Package1;

class Default1 {
    //class Default1 displays the default access modifiers.
    //Even though package was imported but the object was not created due to being default.
    void display(){
        System.out.println("Hello, I am checking the default access modifiers.");
    }
}
